# ──── Server ────
spring.application.name=hms
server.port=8080

# ──── MySQL Datasource ────
spring.datasource.url=jdbc:mysql://localhost:3306/hms_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Karthik@592004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ──── JPA / Hibernate ────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server
server.port=8080

# JWT Configuration
jwt.secret=HotelBookingSystemSecretKeyForJWTTokenGenerationAndValidation2024SuperSecure
jwt.access-token-expiry=900000
jwt.refresh-token-expiry=604800000

# Google OAuth2 (replace with real values)
spring.security.oauth2.client.registration.google.client-id=your-google-client-id
spring.security.oauth2.client.registration.google.client-name=Google
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/api/v1/auth/oauth2/callback/google

# Swagger / SpringDoc
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# CORS
cors.allowed-origins=http://localhost:5173
